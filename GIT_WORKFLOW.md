# Git 两台电脑协同开发工作流程

## 📋 目录
- [初始设置](#初始设置)
- [日常工作流程](#日常工作流程)
- [具体操作步骤](#具体操作步骤)
- [常见问题解决](#常见问题解决)
- [最佳实践](#最佳实践)
- [命令速查表](#命令速查表)

---

## 🚀 初始设置（只需做一次）

### 在电脑A（当前电脑）
```bash
# 1. 初始化仓库并提交代码
cd "/Users/boom/Desktop/my_bussiness"
git add .
git commit -m "Initial commit"

# 2. 在GitHub上创建仓库
# 访问 https://github.com/new 创建新仓库

# 3. 连接远程仓库
git remote add origin https://github.com/YOUR_USERNAME/my_bussiness.git

# 4. 推送代码
git push -u origin main
```

### 在电脑B（另一台电脑）
```bash
# 1. 克隆项目到本地
cd ~/Desktop  # 或你想放置的目录
git clone https://github.com/YOUR_USERNAME/my_bussiness.git

# 2. 进入项目目录
cd my_bussiness

# 3. 查看项目状态
git status
git log --oneline
```

---

## 🔄 日常工作流程

### 标准流程图
```
┌─────────────────────────────────────┐
│  1. 开始工作前：git pull origin main │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  2. 修改代码、开发新功能              │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  3. 查看修改：git status / git diff  │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  4. 添加到暂存区：git add .          │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  5. 提交到本地：git commit -m "..."  │
└─────────────┬───────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  6. 推送到GitHub：git push origin main│
└─────────────────────────────────────┘
```

---

## 💻 具体操作步骤

### 场景1：在电脑A开始工作

```bash
# 第1步：拉取最新代码（确保本地是最新的）
cd "/Users/boom/Desktop/my_bussiness"
git pull origin main

# 第2步：开始修改代码
# 比如修改 ea3.py，添加新功能等...

# 第3步：查看修改了什么
git status              # 查看哪些文件被修改
git diff ea3.py        # 查看具体修改内容

# 第4步：添加到暂存区
git add .              # 添加所有修改
# 或
git add "Foreign company job opportunities/code_exe/ea3.py"  # 添加指定文件

# 第5步：提交到本地仓库
git commit -m "Update ea3.py: Add debug print statements"

# 第6步：推送到GitHub
git push origin main
```

### 场景2：切换到电脑B工作

```bash
# 第1步：拉取电脑A的最新修改
cd ~/Desktop/my_bussiness
git pull origin main

# 现在电脑B的代码已经是最新的，包含电脑A的修改

# 第2步：继续开发
# 修改文件...

# 第3步：提交并推送
git add .
git commit -m "Add ea23.py: New interview question"
git push origin main
```

### 场景3：回到电脑A继续工作

```bash
# 第1步：拉取电脑B的修改
cd "/Users/boom/Desktop/my_bussiness"
git pull origin main

# 现在电脑A已同步电脑B的修改
# 继续开发...
```

---

## 🔧 两台电脑同步流程

```
电脑A                        GitHub                      电脑B
  │                            │                           │
  │─── git push ─────────────→ │                           │
  │                            │                           │
  │                            │ ←──── git pull ──────────│
  │                            │                           │
  │                            │                           │
  │                            │ ←──── git push ──────────│
  │                            │                           │
  │←─── git pull ─────────────│                           │
  │                            │                           │
```

---

## ⚠️ 常见问题解决

### 问题1：忘记pull就开始修改代码

**症状：**
```bash
git push origin main
# error: failed to push some refs
```

**解决方案：**
```bash
# 1. 先拉取远程更改
git pull origin main

# 2a. 如果没有冲突，自动合并成功
# 2b. 如果有冲突，需要手动解决（见下文）
```

### 问题2：出现合并冲突

**症状：**
```bash
git pull origin main
# CONFLICT (content): Merge conflict in ea3.py
```

**解决步骤：**

```bash
# 1. 查看哪些文件冲突
git status

# 2. 打开冲突文件，会看到冲突标记：
# <<<<<<< HEAD
# 电脑A的修改
# print("电脑A的代码")
# =======
# 电脑B的修改
# print("电脑B的代码")
# >>>>>>> origin/main

# 3. 手动编辑，保留需要的代码，删除冲突标记
# 编辑后保存

# 4. 标记冲突已解决
git add ea3.py

# 5. 完成合并
git commit -m "Merge: Resolve conflict in ea3.py"

# 6. 推送
git push origin main
```

### 问题3：误提交了不该提交的文件

```bash
# 从Git中删除但保留本地文件
git rm --cached 文件名

# 或者使用 .gitignore 忽略文件
echo "敏感文件.txt" >> .gitignore
git add .gitignore
git commit -m "Update .gitignore"
```

### 问题4：想撤销最近的提交

```bash
# 撤销最近一次提交，但保留修改
git reset --soft HEAD~1

# 撤销最近一次提交，并丢弃修改
git reset --hard HEAD~1
```

---

## ✅ 最佳实践（避免冲突）

### 黄金规则 🏆

#### 1. 开始工作前必须 pull
```bash
git pull origin main  # 每次开始工作的第一件事
```

#### 2. 经常提交和推送
```bash
# 完成一个小功能就提交
git add .
git commit -m "完成xxx功能"
git push origin main
```

#### 3. 下班前务必推送
```bash
# 确保工作成果推送到GitHub
git push origin main
```

#### 4. 避免同时修改同一个文件
- 如果可能，两台电脑处理不同的文件
- 或者约定时间，一台电脑工作时另一台不动

#### 5. 写清晰的提交信息
```bash
# ✅ 好的提交信息
git commit -m "Fix: 修复ea3.py中的pivot错误"
git commit -m "Add: 新增ea24.py SQL面试题"
git commit -m "Update: 优化ea10.py性能"

# ❌ 不好的提交信息
git commit -m "update"
git commit -m "fix bug"
git commit -m "修改文件"
```

---

## 🔍 命令速查表

| 命令 | 作用 | 使用时机 |
|------|------|---------|
| `git pull origin main` | 拉取最新代码 | **每次开始工作前（最重要！）** |
| `git status` | 查看文件状态 | 随时查看 |
| `git diff` | 查看修改内容 | 提交前检查 |
| `git add .` | 添加所有修改 | 提交前 |
| `git add 文件名` | 添加指定文件 | 只提交部分文件时 |
| `git commit -m "msg"` | 提交到本地 | 完成一个功能后 |
| `git push origin main` | 推送到GitHub | **提交后立即推送** |
| `git log --oneline` | 查看提交历史 | 需要了解历史时 |
| `git log --graph` | 图形化查看历史 | 查看分支关系 |
| `git remote -v` | 查看远程仓库 | 确认远程地址 |
| `git branch` | 查看分支 | 确认当前分支 |

---

## 🎯 推荐的工作流程模板

### 每天开始工作（早上/回家后）
```bash
cd "/Users/boom/Desktop/my_bussiness"
git pull origin main
# 开始写代码...
```

### 每个功能完成后
```bash
git add .
git commit -m "描述你做了什么"
git push origin main
```

### 每天结束工作（下班/睡觉前）
```bash
git status  # 确保没有未提交的修改
git push origin main  # 确保推送到GitHub
```

---

## 💡 实战示例

### 周一早上在电脑A（公司）
```bash
cd "/Users/boom/Desktop/my_bussiness"
git pull origin main
# 修改 ea3.py
git add "Foreign company job opportunities/code_exe/ea3.py"
git commit -m "Update ea3.py: Fix pivot bug"
git push origin main
```

### 周一晚上在电脑B（家里）
```bash
cd ~/Desktop/my_bussiness
git pull origin main  # ← 拿到早上电脑A的修改
# 创建 ea24.py
git add "Foreign company job opportunities/code_exe/ea24.py"
git commit -m "Add ea24.py: New SQL question"
git push origin main
```

### 周二早上在电脑A（公司）
```bash
cd "/Users/boom/Desktop/my_bussiness"
git pull origin main  # ← 拿到昨晚电脑B的修改
# 继续工作...
```

---

## 🚨 重要注意事项

### ✅ 要做的事情
- ✅ 每次开始工作前 `git pull`
- ✅ 频繁提交（小步快跑）
- ✅ 提交信息要清晰
- ✅ 下班前推送代码
- ✅ 遇到问题及时解决

### ❌ 不要做的事情
- ❌ 不要跳过 `git pull`
- ❌ 不要直接在GitHub网页编辑文件
- ❌ 不要长时间不提交（容易忘记改了什么）
- ❌ 不要用 `git push --force`（除非你知道自己在做什么）
- ❌ 不要提交敏感信息（密码、密钥等）

---

## 📚 更多资源

### Git学习资源
- [Git官方文档](https://git-scm.com/doc)
- [GitHub官方教程](https://docs.github.com/cn)
- [Pro Git 中文版](https://git-scm.com/book/zh/v2)

### 常用Git图形化工具
- **VSCode** - 内置Git支持
- **GitHub Desktop** - 简单易用
- **SourceTree** - 功能强大
- **GitKraken** - 界面美观

---

## 📝 快速参考卡片

### 基础三步走
```bash
git pull origin main   # 1. 拉取最新代码
# ... 修改代码 ...
git add .              # 2. 添加修改
git commit -m "xxx"    # 3. 提交说明
git push origin main   # 4. 推送到远程
```

### 检查当前状态
```bash
git status             # 查看文件状态
git log --oneline -5   # 查看最近5条提交
git diff               # 查看未暂存的修改
```

### 紧急情况处理
```bash
git stash              # 暂存当前修改
git pull origin main   # 拉取最新代码
git stash pop          # 恢复暂存的修改
```

---

**遵循这个工作流程，你的两台电脑会始终保持同步！** 🎉

如有问题，随时查阅本文档或联系技术支持。
